const userName = "Anil kumawat";

console.log(userName);
// can not do this because const can not be updated
// userName = "not the name ";

let userId = 39;

console.log(userId);

//  when using let we can update let variable's values

userId = 20;

console.log("new uer Id", userId);

// in var we can also update values of the var variable
var userAddress = "SOME_RANDOM_ADDRESS";

console.log(userAddress);

userAddress = "ANOTHER_RANDOM_ADDRESS";

console.log("new user address is :", userAddress);


// that's all  for now about let ,const and var honestly we will never user var and let would be used at very restricted areas orf scope